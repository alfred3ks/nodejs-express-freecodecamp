/*
Introducción con nodeJS:
nodejs:
Es un entorno de ejecucion de JavaScript orientado a eventos asincronos.

Entorno de ejecucion -> entorno o infraestructura en la cual se ejecuta un programa o aplicacion.
Orientado a eventos asincronos -> Evento que se ejecuta independientemente del proceso principal de la aplicacion.
Por ejemplo para pedir informacion a la BD, eso desencadena un evento asincrono.

Un evento sincrono es aquel que se ejecuta como parte del proceso principal de la apliacion. Estos si pueden bloquear la aplicacion.

Caracteristicas de nodejs:
- Open source,
- Multiplataforma,
- Basado en el motor V8 de google.

Motor V8: es un motor de JS desarrollado por el Cromium Project para Google Chrome.

Motor de JS: Es un software que ejecuta codigo de JS.

Conceptos importantes que debemos saber:
- Arquitectura cliente servidor. Modelo en el cual el servidor envia recuersos al dispositivo que lo solicita (cliente).

- Desarrollo frontend. Area del desarrollo web que se encarga del desarrollo de los componentes que ve el suario y con los cuales interactua.

- Desarrollo backend. Area del desarrollo web que se encarga del dearrollo de los servidores. Lo que el usuario no ve. Los servidores trabajan muy cercanos a las BD. NodeJS lo usamos para el backend.

- Protocolo. Conjunto de Reglas que permiten que dos entidades de una red se comuniquen. En internet usamos el protocolo HTTP y HTTPS. Osea los clientes y servidores se comunican por medio del protocolo HTTP.

- Base de datos. Conjunto de datos estructurados sobre un mismo contexto que se puede consultar.

- Paginas web estaticas. Son paginas web que una vez descargadas en el cliente no necesitan ningun tipo de interaccion con el usuario. No se actualiza nada en ella. Ejemplo un blog. El articulo ya esta ahi y una vez descargada no se modifica.

- Paginas web dinamicas. Si tiene interaccion con el usuario. Y pueden realizar distintas solicitudes a los servidores.

*/